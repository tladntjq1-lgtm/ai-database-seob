# PostgreSQL 접속 정보 예시 파일
# 이 파일을 복사해서 .env 로 만들고 실제 값을 채우세요.
#   PowerShell:  Copy-Item .env.example .env
# .env 파일은 .gitignore 에 등록되어 있어 커밋되지 않습니다.

PGHOST=localhost
PGPORT=5432
PGDATABASE=ai_database_book
PGUSER=your_username_here
PGPASSWORD=your_password_here
